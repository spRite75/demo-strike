type Query {
  hello: String!
  profile: UserProfile
}

type Mutation {
  createProfile(input: CreateProfileInput!): UserProfile!
}

input CreateProfileInput {
    displayName: String!
}

type UserProfile {
  id: ID!
  displayName: String!
  steamId: String
}
